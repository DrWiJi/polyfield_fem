{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build ROCm kernel (gfx1100)",
            "type": "shell",
            "command": "${workspaceFolder}\\build_rocm.bat",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$msCompile"],
            "presentation": {
                "reveal": "silent",
                "panel": "shared"
            }
        }
    ]
}
